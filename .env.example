# ⚠️ CONFIGURAÇÃO DE EXEMPLO - NÃO PREENCHA COM DADOS REAIS
# Este arquivo é apenas um template. Para executar a aplicação:
# 1. Copie este arquivo: cp .env.example .env
# 2. Preencha os valores com suas credenciais reais
# 3. NUNCA faça commit do arquivo .env

# ═══════════════════════════════════════════════════════════════════════════
# 📊 CONFIGURAÇÕES DO BANCO DE DADOS (PostgreSQL)
# ═══════════════════════════════════════════════════════════════════════════
PORT=3000
DB_NAME=budget_generator
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# ═══════════════════════════════════════════════════════════════════════════
# 🔐 CONFIGURAÇÃO DE AUTENTICAÇÃO
# ═══════════════════════════════════════════════════════════════════════════
# Chave secreta para assinar tokens JWT
# Gere uma chave aleatória segura (min 32 caracteres)
# Sugestão: use `openssl rand -hex 32` para gerar
JWT_SECRET=seu_secret_key_aqui

# ═══════════════════════════════════════════════════════════════════════════
# 🤖 CONFIGURAÇÃO DA API DO GEMINI (Google Generative AI)
# ═══════════════════════════════════════════════════════════════════════════
# Obtenha sua chave em: https://ai.google.dev/
# 1. Acesse https://aistudio.google.com/app/apikey
# 2. Clique em "Create API Key"
# 3. Cole a chave aqui
# 4. NUNCA compartilhe esta chave ou faça commit
GEMINI_API_KEY=sua_chave_api_aqui

# ═══════════════════════════════════════════════════════════════════════════
# 📝 INSTRUÇÕES DE SEGURANÇA
# ═══════════════════════════════════════════════════════════════════════════
# 
# ⚠️  IMPORTANTE:
# - O arquivo .env NUNCA deve ser comitado no Git
# - Verifique se .env está no .gitignore (já está configurado)
# - Em produção, use variáveis de ambiente do servidor
# - Rotacione as chaves API regularmente
# 
# ✅ VERIFICAÇÃO:
# 1. Certifique-se de que .env está em .gitignore
#    grep "\.env" .gitignore
# 2. Se já comitou accidentalmente:
#    git rm --cached .env
#    git commit -m "Remove .env file"
#
# 🔄 PARA RESTAURAR CHAVES COMPROMETIDAS:
# 1. Acesse https://aistudio.google.com/app/apikey
# 2. Delete a chave antiga
# 3. Crie uma nova chave
# 4. Atualize o arquivo .env
# 5. Restart a aplicação
